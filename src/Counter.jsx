export const Counter = ({value}) => {
  return (
    <>
      <section>
         <div>
          <h1>Counter App</h1>
          <span>{value}</span>
          <button>Add 1+</button>
         </div>
      </section>
    </>
    )
}